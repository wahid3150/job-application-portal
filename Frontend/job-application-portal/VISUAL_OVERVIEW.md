# 🎯 API INTEGRATION - VISUAL OVERVIEW

## 🏛️ The Complete Architecture

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           REACT FRONTEND (VITE)                              │
│                                                                               │
│  ┌──────────────────────────────────────────────────────────────────────┐   │
│  │                    PAGES & COMPONENTS                                │   │
│  │                                                                       │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐                 │   │
│  │  │   Login     │  │  Signup     │  │ Dashboard   │  ...            │   │
│  │  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘                 │   │
│  │         │                │                │                        │   │
│  │         └────────────────┼────────────────┘                        │   │
│  │                          │                                         │   │
│  │                          ▼                                         │   │
│  │         ┌─────────────────────────────────┐                       │   │
│  │         │   useAuth() Hook                │ ← Get auth state      │   │
│  │         └─────────────────────────────────┘                       │   │
│  │                          │                                         │   │
│  │                          ▼                                         │   │
│  │         ┌─────────────────────────────────┐                       │   │
│  │         │  authAPI, jobAPI, etc.          │ ← API functions       │   │
│  │         │  (from apiService.js)           │                       │   │
│  │         └─────────────────────────────────┘                       │   │
│  │                          │                                         │   │
│  │                          ▼                                         │   │
│  │         ┌─────────────────────────────────┐                       │   │
│  │         │  axiosInstance                  │ ← HTTP client         │   │
│  │         │  - Token injection              │   + interceptors      │   │
│  │         │  - Error handling               │                       │   │
│  │         └─────────────────────────────────┘                       │   │
│  │                          │                                         │   │
│  └──────────────────────────┼─────────────────────────────────────────┘   │
│                             │                                              │
└─────────────────────────────┼──────────────────────────────────────────────┘
                              │
                              │ JSON
                              │ HTTP
                              │
┌─────────────────────────────┼──────────────────────────────────────────────┐
│                             ▼                                               │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │              EXPRESS.JS BACKEND (Port 5000)                         │   │
│  │                                                                      │   │
│  │  ┌──────────────────────────────────────────────────────────────┐  │   │
│  │  │  Routes (6 categories)                                       │  │   │
│  │  │                                                              │  │   │
│  │  │  ✅ POST   /auth/register          → authController         │  │   │
│  │  │  ✅ POST   /auth/login             → authController         │  │   │
│  │  │  ✅ GET    /auth/me (protected)    → authController         │  │   │
│  │  │  ─────────────────────────────────────────────────────────  │  │   │
│  │  │  ✅ GET    /jobs                   → jobController          │  │   │
│  │  │  ✅ GET    /jobs/:id               → jobController          │  │   │
│  │  │  ✅ POST   /jobs (protected)       → jobController          │  │   │
│  │  │  ✅ GET    /jobs/employer/me (prot)→ jobController          │  │   │
│  │  │  ✅ PUT    /jobs/:id (protected)   → jobController          │  │   │
│  │  │  ✅ DELETE /jobs/:id (protected)   → jobController          │  │   │
│  │  │  ✅ PATCH  /jobs/:id/toggle (prot)→ jobController          │  │   │
│  │  │  ─────────────────────────────────────────────────────────  │  │   │
│  │  │  ✅ POST   /applications/:jobId    → applicationController  │  │   │
│  │  │  ✅ GET    /applications/me        → applicationController  │  │   │
│  │  │  ✅ GET    /applications/job/:id   → applicationController  │  │   │
│  │  │  ✅ PATCH  /applications/:id/status→applicationController  │  │   │
│  │  │  ─────────────────────────────────────────────────────────  │  │   │
│  │  │  ✅ POST   /saved-jobs/:jobId      → savedJobController     │  │   │
│  │  │  ✅ GET    /saved-jobs             → savedJobController     │  │   │
│  │  │  ✅ DELETE /saved-jobs/:jobId      → savedJobController     │  │   │
│  │  │  ─────────────────────────────────────────────────────────  │  │   │
│  │  │  ✅ GET    /users/me               → userController         │  │   │
│  │  │  ✅ PUT    /users/me               → userController         │  │   │
│  │  │  ✅ PUT    /users/me/avatar        → userController         │  │   │
│  │  │  ✅ PUT    /users/me/resume        → userController         │  │   │
│  │  │  ✅ DELETE /users/me/resume        → userController         │  │   │
│  │  │  ✅ GET    /users/:id              → userController         │  │   │
│  │  │  ─────────────────────────────────────────────────────────  │  │   │
│  │  │  ✅ GET    /analytics/me           → analyticsController    │  │   │
│  │  │                                                              │  │   │
│  │  └──────────────────────────────────────────────────────────────┘  │   │
│  │                                                                      │   │
│  │  ┌──────────────────────────────────────────────────────────────┐  │   │
│  │  │  Middleware (Security Layer)                                 │  │   │
│  │  │  - protect: Verify JWT token                                 │  │   │
│  │  │  - authorizeRoles: Check user role (employer/jobseeker)      │  │   │
│  │  │  - validate: Validate request body                           │  │   │
│  │  │  - upload: Handle file uploads                               │  │   │
│  │  └──────────────────────────────────────────────────────────────┘  │   │
│  │                                                                      │   │
│  │  ┌──────────────────────────────────────────────────────────────┐  │   │
│  │  │  Database (MongoDB)                                          │  │   │
│  │  │  ├─ Users Collection                                         │  │   │
│  │  │  │  ├─ name, email, password (hashed)                        │  │   │
│  │  │  │  ├─ role (employer/jobseeker)                             │  │   │
│  │  │  │  ├─ avatar URL, resume URL                                │  │   │
│  │  │  │  ├─ companyName, companyDescription                       │  │   │
│  │  │  │  └─ timestamps                                            │  │   │
│  │  │  │                                                            │  │   │
│  │  │  ├─ Jobs Collection                                          │  │   │
│  │  │  │  ├─ title, description, requirements                      │  │   │
│  │  │  │  ├─ employer_id (FK)                                      │  │   │
│  │  │  │  ├─ salary, location, jobType                             │  │   │
│  │  │  │  ├─ isActive (toggle status)                              │  │   │
│  │  │  │  └─ timestamps                                            │  │   │
│  │  │  │                                                            │  │   │
│  │  │  ├─ Applications Collection                                  │  │   │
│  │  │  │  ├─ jobId (FK), userId (FK)                              │  │   │
│  │  │  │  ├─ status (pending/accepted/rejected)                    │  │   │
│  │  │  │  └─ timestamps                                            │  │   │
│  │  │  │                                                            │  │   │
│  │  │  └─ SavedJobs Collection                                     │  │   │
│  │  │     ├─ jobId (FK), userId (FK)                              │  │   │
│  │  │     └─ timestamps                                            │  │   │
│  │  │                                                              │  │   │
│  │  └──────────────────────────────────────────────────────────────┘  │   │
│  │                                                                      │   │
│  └──────────────────────────────────────────────────────────────────────┘  │
│                                                                               │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 📱 Component Integration Map

```
                        App.jsx
                    (AuthProvider)
                          │
        ┌─────────────────┼─────────────────┐
        │                 │                 │
        ▼                 ▼                 ▼
    PUBLIC ROUTES    PROTECTED ROUTES  SHARED STATE
        │                 │                 │
        ├─ /               │            AuthContext
        ├─ /login      ┌──▶│ /employer-dashboard
        ├─ /signup     │   │ /post-job
        ├─ /find-jobs  │   │ /manage-jobs
        ├─ /job/:id    │   │ /application
        ├─ /profile    │   │ /company-profile
        └─ /saved-jobs │   │
                        │   └─ ProtectedRoute
                        │      (Checks auth & role)
                        │
                    useAuth()
                        │
        ┌───────────────┼───────────────┐
        │               │               │
        ▼               ▼               ▼
    useContext      user state      token state
    └─ login()      └─ user data    └─ JWT token
    └─ logout()

    ┌──────────────────────────────────────┐
    │      API SERVICE LAYER               │
    │                                      │
    │  Import from apiService.js:         │
    │  ├─ authAPI                         │
    │  ├─ jobAPI                          │
    │  ├─ applicationAPI                  │
    │  ├─ savedJobAPI                     │
    │  ├─ userAPI                         │
    │  └─ analyticsAPI                    │
    └──────────────────────────────────────┘
           │
           ▼
    ┌──────────────────────────────────────┐
    │    AXIOS INSTANCE                    │
    │                                      │
    │  Request Interceptor:                │
    │  ├─ Get token from localStorage      │
    │  ├─ Add Authorization header         │
    │  └─ Send request                     │
    │                                      │
    │  Response Interceptor:               │
    │  ├─ Check status                     │
    │  ├─ Handle 401 (redirect)            │
    │  └─ Return data or error             │
    └──────────────────────────────────────┘
```

---

## 🔄 Data Flow Examples

### Example 1: Job Seeker Browsing Jobs

```
┌─ User navigates to /find-jobs ─────────────────────────────┐
│                                                             │
│  1. JobSeekerDashboard component mounts                   │
│  2. useEffect hook runs                                   │
│  3. Calls: jobAPI.getAllJobs()                            │
│                                                            │
└─────────────────────┬─────────────────────────────────────┘
                      │
                      ▼
┌─ apiService.js (jobAPI module) ────────────────────────────┐
│                                                             │
│  const getAllJobs = async (filters = {}) => {             │
│    const response = await axiosInstance.get(              │
│      "http://localhost:5000/api/jobs",                    │
│      { params: filters }                                  │
│    );                                                      │
│    return response.data;                                  │
│  }                                                         │
│                                                            │
└─────────────────────┬─────────────────────────────────────┘
                      │
                      ▼
┌─ axiosInstance.js (Request Interceptor) ───────────────────┐
│                                                             │
│  1. Check localStorage for token                          │
│  2. Token exists: "eyJ0eXAiOiJKV1QiLCJhbGc..."           │
│  3. Add header: "Authorization: Bearer [token]"           │
│  4. Send GET request to /jobs                            │
│                                                            │
└─────────────────────┬─────────────────────────────────────┘
                      │
                      ▼
┌─ Backend Express.js (Route: GET /jobs) ────────────────────┐
│                                                             │
│  router.get("/", getAllJobs)                              │
│                                                            │
│  const getAllJobs = async (req, res) => {                │
│    try {                                                   │
│      const jobs = await Job.find({ isActive: true })    │
│      res.json({ success: true, jobs })                   │
│    } catch(error) {                                       │
│      res.status(500).json({ success: false })            │
│    }                                                       │
│  }                                                         │
│                                                            │
└─────────────────────┬─────────────────────────────────────┘
                      │
                      ▼
┌─ Database (MongoDB) ────────────────────────────────────────┐
│                                                             │
│  Find all documents from Jobs collection                   │
│  Return: [{ _id, title, desc, salary, ... }, ...]        │
│                                                            │
└─────────────────────┬─────────────────────────────────────┘
                      │
                      ▼ (Response JSON)
┌─ Backend → Network ─────────────────────────────────────────┐
│                                                             │
│  { success: true, jobs: [{...}, {...}, ...] }             │
│                                                            │
└─────────────────────┬─────────────────────────────────────┘
                      │
                      ▼
┌─ axiosInstance.js (Response Interceptor) ──────────────────┐
│                                                             │
│  1. Check status (200) ✅                                  │
│  2. Return response.data                                  │
│                                                            │
└─────────────────────┬─────────────────────────────────────┘
                      │
                      ▼
┌─ JobSeekerDashboard Component ──────────────────────────────┐
│                                                             │
│  const response = await jobAPI.getAllJobs()               │
│  setJobs(response.jobs)  // Update state                  │
│  Component re-renders with job list                       │
│                                                            │
└─────────────────────┬─────────────────────────────────────┘
                      │
                      ▼
┌─ User Sees Job Listings ────────────────────────────────────┐
│                                                             │
│  ┌─────────────────────────────────────┐                  │
│  │ Senior Developer @ Google            │ [Apply] [Save]  │
│  │ $150K/yr | New York, NY             │                  │
│  └─────────────────────────────────────┘                  │
│                                                            │
│  ┌─────────────────────────────────────┐                  │
│  │ Product Manager @ Microsoft           │ [Apply] [Save]  │
│  │ $180K/yr | Seattle, WA              │                  │
│  └─────────────────────────────────────┘                  │
│                                                            │
└────────────────────────────────────────────────────────────┘
```

### Example 2: Job Seeker Applying for a Job

```
┌─ User clicks [Apply] button ────────────────────────────────┐
│                                                             │
│  1. handleApply(jobId) function triggers                  │
│  2. Calls: applicationAPI.applyForJob(jobId)             │
│                                                            │
└─────────────────────┬─────────────────────────────────────┘
                      │
                      ▼
┌─ apiService.js (applicationAPI) ──────────────────────────┐
│                                                             │
│  const applyForJob = async (jobId) => {                   │
│    const response = await axiosInstance.post(            │
│      "http://localhost:5000/api/applications/" + jobId   │
│    );                                                      │
│    return response.data;                                  │
│  }                                                         │
│                                                            │
└─────────────────────┬─────────────────────────────────────┘
                      │
                      ▼
┌─ axiosInstance Interceptor ────────────────────────────────┐
│                                                             │
│  1. Add token header                                       │
│  2. Send POST to /applications/[jobId]                    │
│  3. Body: empty (token proves identity)                   │
│                                                            │
└─────────────────────┬─────────────────────────────────────┘
                      │
                      ▼
┌─ Backend (POST /applications/:jobId) ──────────────────────┐
│                                                             │
│  middleware: protect → Verify token → req.user.id         │
│                                                            │
│  const applyJob = async (req, res) => {                  │
│    const { jobId } = req.params;                         │
│    const userId = req.user.id;  // From token            │
│                                                            │
│    // Check if already applied                            │
│    const existing = await Application.findOne({           │
│      jobId, userId                                        │
│    });                                                    │
│    if (existing) {                                        │
│      return res.status(400).json({                       │
│        success: false,                                   │
│        message: "Already applied"                        │
│      });                                                 │
│    }                                                      │
│                                                            │
│    // Create application                                  │
│    const app = new Application({                          │
│      jobId,                                               │
│      userId,                                              │
│      status: "pending"                                    │
│    });                                                    │
│    await app.save();                                      │
│                                                            │
│    res.json({                                             │
│      success: true,                                       │
│      message: "Application submitted!"                    │
│    });                                                    │
│  }                                                         │
│                                                            │
└─────────────────────┬─────────────────────────────────────┘
                      │
                      ▼
┌─ Database ──────────────────────────────────────────────────┐
│                                                             │
│  Create new Application document:                         │
│  {                                                         │
│    _id: "507f1f77bcf86cd799439011",                      │
│    jobId: "507f1f77bcf86cd799439012",                    │
│    userId: "507f1f77bcf86cd799439013",                   │
│    status: "pending",                                     │
│    createdAt: 2024-01-20T10:30:00.000Z                  │
│  }                                                         │
│                                                            │
└─────────────────────┬─────────────────────────────────────┘
                      │
                      ▼
┌─ Response Back to Frontend ─────────────────────────────────┐
│                                                             │
│  { success: true, message: "Application submitted!" }     │
│                                                            │
└─────────────────────┬─────────────────────────────────────┘
                      │
                      ▼
┌─ Component ──────────────────────────────────────────────────┐
│                                                             │
│  const response = await applicationAPI.applyForJob(jobId) │
│  if (response.success) {                                   │
│    toast.success("Application submitted!");              │
│    setApplied(true);  // Disable button                   │
│  } else {                                                  │
│    toast.error(response.message);                         │
│  }                                                         │
│                                                            │
└─────────────────────┬─────────────────────────────────────┘
                      │
                      ▼
┌─ User Feedback ──────────────────────────────────────────────┐
│                                                             │
│  Toast: "✅ Application submitted!"                        │
│  [Apply] button changes to [Applied] (disabled)           │
│                                                            │
└────────────────────────────────────────────────────────────┘
```

---

## 🔐 Authentication Sequence

```
┌─ FIRST TIME USER ───────────────────────────────────────────┐
│                                                             │
│  User clicks "Sign Up"                                    │
│     │                                                      │
│     ▼                                                      │
│  Fills form:                                              │
│  ├─ Full Name                                             │
│  ├─ Email                                                 │
│  ├─ Password                                              │
│  ├─ Role (Job Seeker / Employer)                          │
│  └─ Profile Picture                                       │
│     │                                                      │
│     ▼                                                      │
│  Calls: authAPI.register(userData)                        │
│     │                                                      │
│     ▼                                                      │
│  Backend creates user, hashes password, returns success   │
│     │                                                      │
│     ▼                                                      │
│  Shows: "Account created! Please login"                   │
│     │                                                      │
│     ▼                                                      │
│  Redirect to /login                                       │
│                                                            │
└────────────────────────────────────────────────────────────┘

┌─ RETURNING USER ────────────────────────────────────────────┐
│                                                             │
│  User clicks "Login"                                      │
│     │                                                      │
│     ▼                                                      │
│  Fills form:                                              │
│  ├─ Email                                                 │
│  └─ Password                                              │
│     │                                                      │
│     ▼                                                      │
│  Calls: authAPI.login(email, password)                    │
│     │                                                      │
│     ▼                                                      │
│  Backend:                                                 │
│  ├─ Finds user by email                                  │
│  ├─ Compares password with hash (bcrypt)                 │
│  ├─ If match → generates JWT token (7 days)             │
│  └─ Returns: { success: true, token, user }             │
│     │                                                      │
│     ▼                                                      │
│  Frontend:                                                │
│  ├─ localStorage.setItem("token", token)                │
│  ├─ localStorage.setItem("user", JSON.stringify(user))   │
│  ├─ AuthContext.login(user, token)                       │
│  └─ Shows success animation                              │
│     │                                                      │
│     ▼                                                      │
│  Redirects based on role:                                 │
│  ├─ Employer → /employer-dashboard                       │
│  └─ Job Seeker → /find-jobs                              │
│                                                            │
│  Status: ✅ LOGGED IN                                     │
│                                                            │
└────────────────────────────────────────────────────────────┘

┌─ MAKING PROTECTED API CALLS ────────────────────────────────┐
│                                                             │
│  Logged-in user makes any API request                     │
│     │                                                      │
│     ▼                                                      │
│  axiosInstance intercepts request                         │
│  ├─ Gets token from localStorage                         │
│  ├─ Adds header: "Authorization: Bearer [token]"        │
│  └─ Sends request with auth                              │
│     │                                                      │
│     ▼                                                      │
│  Backend middleware (protect):                            │
│  ├─ Extracts token from header                           │
│  ├─ Verifies JWT signature                               │
│  ├─ Decodes token → gets userId                          │
│  ├─ Attaches user to request (req.user)                 │
│  └─ Calls route handler                                  │
│     │                                                      │
│     ▼                                                      │
│  Route handler processes request with user context       │
│  └─ Returns data                                          │
│     │                                                      │
│     ▼                                                      │
│  Response sent back to frontend                          │
│     │                                                      │
│     ▼                                                      │
│  Component receives data and updates UI                  │
│                                                            │
│  Status: ✅ AUTHENTICATED REQUEST SUCCESSFUL              │
│                                                            │
└────────────────────────────────────────────────────────────┘

┌─ SESSION EXPIRES ───────────────────────────────────────────┐
│                                                             │
│  Token is valid for 7 days                                │
│  After 7 days, token becomes invalid                      │
│     │                                                      │
│     ▼                                                      │
│  User tries to make API request with expired token        │
│     │                                                      │
│     ▼                                                      │
│  Backend returns: 401 Unauthorized                        │
│     │                                                      │
│     ▼                                                      │
│  axiosInstance response interceptor catches 401          │
│  ├─ localStorage.removeItem("token")                      │
│  ├─ localStorage.removeItem("user")                       │
│  ├─ window.location.href = "/login"                       │
│  └─ Auto-redirect to login page                           │
│     │                                                      │
│     ▼                                                      │
│  User sees login form again                              │
│                                                            │
│  Status: 🔄 SESSION EXPIRED - REDIRECT TO LOGIN           │
│                                                            │
└────────────────────────────────────────────────────────────┘
```

---

## 📊 API Endpoints By Category

```
╔════════════════════════════════════════════════════════════╗
║               AUTHENTICATION (3 endpoints)                  ║
╠════════════════════════════════════════════════════════════╣
║ POST   /auth/register         | Public   | Create account  ║
║ POST   /auth/login            | Public   | Login           ║
║ GET    /auth/me               | Private  | Get profile     ║
╚════════════════════════════════════════════════════════════╝

╔════════════════════════════════════════════════════════════╗
║                  JOB MANAGEMENT (7 endpoints)               ║
╠════════════════════════════════════════════════════════════╣
║ GET    /jobs                  | Public   | Get all jobs    ║
║ GET    /jobs/:id              | Public   | Get single job  ║
║ POST   /jobs                  | Private  | Create job      ║
║ GET    /jobs/employer/me       | Private  | My jobs         ║
║ PUT    /jobs/:id              | Private  | Update job      ║
║ DELETE /jobs/:id              | Private  | Delete job      ║
║ PATCH  /jobs/:id/toggle       | Private  | Toggle status   ║
╚════════════════════════════════════════════════════════════╝

╔════════════════════════════════════════════════════════════╗
║             APPLICATIONS (4 endpoints)                      ║
╠════════════════════════════════════════════════════════════╣
║ POST   /applications/:jobId   | Private  | Apply for job   ║
║ GET    /applications/me       | Private  | My apps         ║
║ GET    /applications/job/:id  | Private  | View applicants ║
║ PATCH  /applications/:id/status | Private| Update status   ║
╚════════════════════════════════════════════════════════════╝

╔════════════════════════════════════════════════════════════╗
║             SAVED JOBS (3 endpoints)                        ║
╠════════════════════════════════════════════════════════════╣
║ POST   /saved-jobs/:jobId     | Private  | Save job        ║
║ GET    /saved-jobs            | Private  | Get saved jobs  ║
║ DELETE /saved-jobs/:jobId     | Private  | Remove saved    ║
╚════════════════════════════════════════════════════════════╝

╔════════════════════════════════════════════════════════════╗
║             USER PROFILE (6 endpoints)                      ║
╠════════════════════════════════════════════════════════════╣
║ GET    /users/me              | Private  | Get my profile  ║
║ PUT    /users/me              | Private  | Update profile  ║
║ PUT    /users/me/avatar       | Private  | Upload avatar   ║
║ PUT    /users/me/resume       | Private  | Upload resume   ║
║ DELETE /users/me/resume       | Private  | Delete resume   ║
║ GET    /users/:id             | Public   | Get pub profile ║
╚════════════════════════════════════════════════════════════╝

╔════════════════════════════════════════════════════════════╗
║             ANALYTICS (1 endpoint)                          ║
╠════════════════════════════════════════════════════════════╣
║ GET    /analytics/me          | Private  | Get metrics     ║
╚════════════════════════════════════════════════════════════╝

╔════════════════════════════════════════════════════════════╗
║                  TOTAL: 24 ENDPOINTS                        ║
╚════════════════════════════════════════════════════════════╝
```

---

## ✅ Integration Checklist

```
INFRASTRUCTURE READY
  ✅ apiPaths.js (Endpoint definitions)
  ✅ axiosInstance.js (HTTP client with interceptors)
  ✅ apiService.js (31 API functions organized by feature)
  ✅ AuthContext.jsx (Global auth state management)
  ✅ ProtectedRoute.jsx (Route protection with role check)

PAGES UPDATED
  ✅ App.jsx (Wrapped with AuthProvider)
  ✅ Login.jsx (Connected to authAPI.login())
  ✅ Signup.jsx (Connected to authAPI.register())

READY TO BUILD PAGES
  ⏳ JobSeekerDashboard (use jobAPI, savedJobAPI)
  ⏳ JobDetails (use jobAPI.getJobById())
  ⏳ SavedJobs (use savedJobAPI)
  ⏳ EmployerDashboard (use jobAPI, analyticsAPI)
  ⏳ JobPostingForm (use jobAPI.createJob())
  ⏳ ManageJobs (use jobAPI)
  ⏳ ApplicationViewer (use applicationAPI)
  ⏳ UserProfile (use userAPI)
  ⏳ EmployerProfilePage (use userAPI)
```

---

**All infrastructure is ready! Start building components using the API functions! 🚀**
