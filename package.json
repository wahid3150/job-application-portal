{
  "name": "job-application-system",
  "version": "1.0.0",
  "description": "Job Application Portal",
  "main": "Backend/server.js",
  "scripts": {
    "start": "node Backend/server.js",
    "dev": "cd Backend && nodemon server.js",
    "build": "cd Frontend/job-application-portal && npm run build",
    "heroku-postbuild": "cd Frontend/job-application-portal && npm install --include=dev && npm run build"
  },
  "engines": {
    "node": "20.x",
    "npm": "9.x"
  },
  "dependencies": {},
  "devDependencies": {}
}
